Author: Dian Chen
Hw01 for CIS560 17fall

1) The tests are divided into several parts since they are quite long to display on the console. Use enter to proceed to a next part.

2) Be careful that the function for generating rotation matrices accepts angles in degrees.

3) Some results are obvious. The less obvious ones are manually checked in Matlab and a plain text confirmation is printed.

4) cross product between two vec4 objects is done by taking cross product of the first three elements, leaving the fourth element alone (just to make this notification because I’ve seen implementations in other courses that normalize the fourth element first).